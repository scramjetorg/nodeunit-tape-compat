#!/usr/bin/env node

const { DataStream } = require('scramjet');
const path = require('path');
const tapeRunner = require('../');

DataStream.from([{path: path.resolve(process.cwd(), process.argv[2])}])
    .pipe(tapeRunner({timeout: 10000}));
